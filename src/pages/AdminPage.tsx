export default function AdminPage() {
  return (
    <section className="card">
      <h2>Admin</h2>
      <p>Funnel, SLA, importstatus for benchmark snapshots och lead-kvalitet.</p>
    </section>
  );
}
